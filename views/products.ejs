<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Benvingut</title>
    <link rel="stylesheet" type="text/css" href="/styles.css">
    <script src="./public/js/deleteProduct.js" defer type="module"></script>
</head>
<body>
    <header>
        <h1>Gestió de Productes per <%= user.name %>!</h1>
        <h2><%- htmlMessage %></h2>
    </header>
    <main>
        <section>
            <h2>Llista de Productes</h2>
            <ul class="product-list">
                <% data.products.forEach(product => { %>
                    <li class="product-item">
                        <strong><%= product.name %></strong>
                        <!--
                        <span>Categoria: <%= product.category %></span>
                        <span>Preu: €<%= product.price %></span>-->
                        <span><a href="/products/<%=product.id %>">Detall producte</a></span>
                        <span><a href="/products/editProducte/<%=product.id %>">Editar producte</a></span>
                        <button onclick="deleteProduct('<%= product.id %>')">Eliminar</button>
                    </li>
                <% }) %>
            </ul>
        </section>
    </main>
</body>
</html>
